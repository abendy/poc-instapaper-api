<div id="bookmarks">
    <ul class="demo-list-item mdl-list">
        {% for bookmark in bookmarks %}
        {% if bookmark.title %}
        <li>
            <a href='?bid={{ bookmark.bookmark_id }}'>{{ bookmark.title|raw }}</a>

            {% if bookmark.starred %}
            &nbsp;&nbsp;<i class="material-icons">star</i>
            {% endif %}

            {% if bookmark.highlights %}
            &nbsp;&nbsp;<i class="material-icons">format_quote</i>
            {% endif %}
        </li>
        {% endif %}
        {% endfor %}
    </ul>
</div>
